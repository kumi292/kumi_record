<h1>記事一覧</h1>
<p>記事の一覧を表示します</p>
<%= link_to '記事作成', new_article_path %>
<div>
  <% @articles.each do |article| %>
    <div>
      <%= link_to article.title, article_path(article.id) %>
    </div>
  <% end %>
</div>
